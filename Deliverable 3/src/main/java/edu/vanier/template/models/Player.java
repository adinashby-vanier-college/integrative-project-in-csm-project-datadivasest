package edu.vanier.template.models;

import javafx.scene.image.Image;

public class Player extends Sprite{
    public Player(int x, int y, int sizeX, int sizeY, Image image) {
        super(x, y, "player", sizeX, sizeY, image);
    }
}
